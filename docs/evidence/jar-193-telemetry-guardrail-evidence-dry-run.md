# JAR-193 Trial Reminder Guardrail Evidence Bundle

Generated at: `2026-04-11T05:10:37.406Z`
Mode: `dry-run`

Issue: [JAR-193](/JAR/issues/JAR-193)
Validation lane: [JAR-189](/JAR/issues/JAR-189)
Parent: [JAR-186](/JAR/issues/JAR-186)

## Summary

| Check | Status | Timestamp (UTC) |
| --- | --- | --- |
| Cron route accessibility probe | SKIP | 2026-04-11T05:10:37.404Z |
| Redirect URL + UTM preservation verification | SKIP | 2026-04-11T05:10:37.405Z |
| Webhook telemetry organization_slug integrity query | SKIP | 2026-04-11T05:10:37.405Z |

## 1. Cron route accessibility probe

- Status: **SKIP**
- Observed at: `2026-04-11T05:10:37.404Z`

Command:

```bash
curl -sS -o /tmp/trial-reminder-cron-body.txt -D /tmp/trial-reminder-cron-headers.txt \
  -H "Authorization: Bearer $CRON_SECRET" \
  "$APP_URL/api/cron/trial-reminders" \
  -w "http_status=%{http_code}\n"
```

Observed:

```text
Dry-run mode: network probe skipped. Run without --dry-run and with APP_URL + CRON_SECRET to execute.
```

## 2. Redirect URL + UTM preservation verification

- Status: **SKIP**
- Observed at: `2026-04-11T05:10:37.405Z`

Command:

```bash
curl -sS -I \
  "$APP_URL/dashboard?utm_source=guardrail&utm_medium=qa&utm_campaign=trial_reminder"
```

Observed:

```text
Dry-run mode: redirect probe skipped. Run without --dry-run to validate status + Location header.
```

## 3. Webhook telemetry organization_slug integrity query

- Status: **SKIP**
- Observed at: `2026-04-11T05:10:37.405Z`

Command:

```bash
# SQL scaffold (execute with psql or your DB client):
select
  count(*)::int as total_rows,
  count(*) filter (where organization_slug is null or btrim(organization_slug) = '')::int as null_or_empty_slug_rows,
  count(*) filter (where organization_slug like 'unknown-org-%')::int as fallback_slug_rows,
  max(created_at) as latest_row_at
from funnel_events
where location = 'stripe_webhook';
```

Observed:

```text
Dry-run mode: database query skipped. SQL scaffold is included below for production execution.
```

## Closure note

This bundle is generated by `yarn telemetry:guardrails:evidence` and is formatted for direct paste into [JAR-189](/JAR/issues/JAR-189) closure comments.